# ============================================================
# Configuración del backend - EJEMPLO (no usar secretos reales)
# Copia este archivo a .env y completa los valores.
# ============================================================

# Entorno: development | test | production
NODE_ENV=development

# Puerto HTTP del backend
PORT=3000

# Cadena de conexión a PostgreSQL
DATABASE_URL=postgresql://usuario:password@localhost:5432/finanzas_dev

# Secreto para firmar los JWT (genera uno fuerte, p.ej. con: openssl rand -hex 64)
JWT_SECRET=reemplazar-con-un-secreto-fuerte-de-al-menos-32-caracteres

# Duración del JWT de acceso (formato de jsonwebtoken: 15m, 1h, 7d)
# Recomendado: corto (15m). El refresh token renueva la sesión mientras haya actividad.
JWT_EXPIRES_IN=15m

# Duración máxima absoluta del refresh token (límite superior de la sesión).
# La sesión también caduca antes si se supera SESSION_IDLE_TIMEOUT sin actividad.
JWT_REFRESH_EXPIRES_IN=7d

# Tiempo máximo de inactividad antes de que la sesión deje de poder renovarse.
# Formato legible: 30m, 1h, 7d (también acepta ms/s/h/d).
SESSION_IDLE_TIMEOUT=30m

# Nombre de la cookie HttpOnly que transporta el JWT
COOKIE_NAME=finanzas_auth

# Cookie segura (solo HTTPS). TRUE en producción, FALSE en desarrollo local
COOKIE_SECURE=false

# Orígenes permitidos para CORS (separados por coma)
CORS_ORIGIN=http://localhost:4200

# Coste del hash de contraseñas (bcrypt)
BCRYPT_ROUNDS=12

# Credenciales del usuario administrador inicial (solo para el script de seed)
ADMIN_EMAIL=admin@finanzas.local
ADMIN_PASSWORD=cambiar-esto-en-el-seed

# ============================================================
# Auth0 (login/registro con Google) — opcional
# Sin estos valores, /auth/google responde GOOGLE_AUTH_NOT_CONFIGURED
# y el resto de la autenticación sigue funcionando con normalidad.
# ============================================================

# Dominio del tenant de Auth0 (sin https://), p.ej. tu-tenant.us.auth0.com
AUTH0_DOMAIN=

# Client ID de la aplicación SPA registrada en Auth0 (es la audience del ID Token)
AUTH0_CLIENT_ID=

# Ignorar el SSl/TLS obligando a node a confiar en la conexion (solo utilizar en desarrollo)
NODE_TLS_REJECT_UNAUTHORIZED="0"